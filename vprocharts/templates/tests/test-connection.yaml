apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vproapp.fullname" . }}-test-connection"
  labels:
    {{- include "vproapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vproapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
