apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vprochart.fullname" . }}-test-connection"
  labels:
    {{- include "vprochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vprochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
